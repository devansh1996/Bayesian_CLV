# ─────────────────────────────────────────────────────────────────────────────
# bayesCLV — thesis environment
# Python 3.10 / 3.11 / 3.12  (do NOT use 3.13+ — PyMC 5.x not yet supported)
#
# WSL / Ubuntu setup (run once before pip install):
#
#   sudo apt update && sudo apt install -y \
#       build-essential python3-dev \
#       libhdf5-dev libnetcdf-dev \
#       python3-venv
#
#   python3.11 -m venv .venv
#   source .venv/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Bayesian stack ────────────────────────────────────────────────────────────
pymc>=5.10,<6.0
pymc-marketing>=0.7,<0.9
arviz>=0.17,<1.0

# ── Numerics ─────────────────────────────────────────────────────────────────
numpy>=1.24,<2.0
scipy>=1.10
pandas>=2.0
xarray>=2023.1

# ── Machine learning baselines ────────────────────────────────────────────────
scikit-learn>=1.3
xgboost>=2.0

# ── Plotting ─────────────────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13

# ── Data I/O ──────────────────────────────────────────────────────────────────
openpyxl>=3.1          # read .xlsx dataset
pyarrow>=14.0          # parquet engine for data/processed/*.parquet
netCDF4>=1.6           # save/load MCMC traces (.nc files)
h5netcdf>=0.14         # fallback NetCDF4 backend

# ── Notebooks & utilities ────────────────────────────────────────────────────
jupyterlab>=4.0
ipykernel>=6.0
tqdm>=4.65
